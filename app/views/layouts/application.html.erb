<!DOCTYPE html>
<html>
  <head>
    <title>CommonBlog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

  <body style="background-color: #171515; color:white;">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <%= link_to 'Home', root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to 'Create New Tasks List', new_tasks_list_path, class: "nav-link" unless current_page?(new_tasks_list_path)  %>
              </li>
            <% end %>
            <li class="nav-item">
              <%= link_to 'Sign in', new_user_session_path, class: "nav-link" unless user_signed_in?%>
            </li>
            <li class="nav-item">
              <%= button_to 'Log out', destroy_user_session_path, method: :delete, class: "nav-link" if user_signed_in? %>
            </li>
          </ul>
        </div>
      </div>
    </nav>


    <%= yield%>

  </body>
</html>
