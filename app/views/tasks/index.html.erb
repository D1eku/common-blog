<% @tasks.each do | task | %>
  <ol>
    <li>
      <div>
        <h1><%= task.title %></h1>
        <%= link_to "Edit", edit_task_path(task) %>
        <p><%= task.description %></p>
        <p><%= task.is_done? %></p>
      </div>
    </li>
  </ol>
<% end %>